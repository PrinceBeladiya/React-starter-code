import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    palette: {
      primary: {
        main: '#2EC4B6',
      },
      secondary: {
        main: '#19857b',
      },
      error: {
        main: '#FF0000',
      },
    },
  },
});

export default theme;
